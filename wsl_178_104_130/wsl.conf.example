[network]
generateResolvConf = false

# VPN selection is manual. Do not apply the VLESS killswitch at WSL boot.
# vless-wsl use PROFILE applies its own fail-closed rules explicitly.
[boot]
command = "/usr/local/sbin/wsl-base-boot"
